fastapi==0.115.0
uvicorn[standard]==0.31.0
pydantic==2.9.2
pydantic-settings==2.5.2
httpx==0.28.1

# Claude API for transcript summarisation
anthropic>=0.34.0

# BullMQ job consumer (official Python client)
bullmq>=2.0.0

# Redis client
redis>=5.0.0

# MinIO / S3-compatible storage
minio>=7.2.0

# ───────────────────────────────────────────────────────────────────────────────
# Heavy ML deps — uncomment when enabling real transcription / PII redaction.
# These pull in torch and take several minutes to install; keep commented out
# in dev unless you have a GPU-capable host.
#
# openai-whisper>=20231117
# whisperx>=3.1.0          # diarization-aware fork of openai-whisper
#
# presidio-analyzer>=2.2.0
# presidio-anonymizer>=2.2.0
# spacy>=3.7.0
# # After installing: python -m spacy download ro_core_news_sm en_core_web_sm
# ───────────────────────────────────────────────────────────────────────────────
