server {
  listen       ${PORT};
  server_name  _;
  root         /usr/share/nginx/html;
  index        index.html;
  # SPA fallback: client-side routing (React Router / TanStack Router).
  location / { try_files $uri $uri/ /index.html; }
}
