/**
 * i18n entry point — Phase 0 STUB.
 *
 * Catalog files exist in `./ro/*.json` și `./en/*.json`, dar wiring-ul efectiv
 * (init i18next, language detector, lazy load namespace, provider) e amânat
 * pentru task-ul frontend-engineer.
 *
 * Vezi `./README.md` secțiunea „Wiring rămas" pentru pași concreți.
 *
 * Până atunci, componentele rămân cu string-uri RO hardcodate (status quo).
 * NU importa nimic din acest fișier — exporturile sunt placeholdere care
 * vor fi populate ulterior.
 */

// Placeholder export pentru a face fișierul un module ES valid.
export const I18N_READY = false;
