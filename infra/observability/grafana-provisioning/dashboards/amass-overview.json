{
  "annotations": { "list": [] },
  "editable": true,
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 6, "w": 8, "x": 0, "y": 0 },
      "id": 1,
      "targets": [
        {
          "expr": "sum(rate(http_requests_total{job=\"amass-api\"}[5m]))",
          "legendFormat": "req/s",
          "refId": "A"
        }
      ],
      "title": "API request rate (5m)",
      "type": "stat"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "percent" }, "overrides": [] },
      "gridPos": { "h": 6, "w": 8, "x": 8, "y": 0 },
      "id": 2,
      "targets": [
        {
          "expr": "100 * sum(rate(http_requests_total{job=\"amass-api\",status=~\"5..\"}[5m])) / sum(rate(http_requests_total{job=\"amass-api\"}[5m]))",
          "legendFormat": "5xx %",
          "refId": "A"
        }
      ],
      "title": "API 5xx error rate",
      "type": "stat"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 6, "w": 8, "x": 16, "y": 0 },
      "id": 3,
      "targets": [
        {
          "expr": "sum(rate(http_requests_total{job=\"amass-api\",status=\"429\"}[5m]))",
          "legendFormat": "429/s",
          "refId": "A"
        }
      ],
      "title": "Rate-limit denials",
      "type": "stat"
    }
  ],
  "refresh": "30s",
  "schemaVersion": 39,
  "tags": ["amass"],
  "time": { "from": "now-1h", "to": "now" },
  "title": "AMASS-CRM — Overview",
  "uid": "amass-overview",
  "version": 1
}
